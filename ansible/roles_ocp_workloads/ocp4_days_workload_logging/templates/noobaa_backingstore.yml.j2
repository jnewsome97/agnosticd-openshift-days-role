apiVersion: noobaa.io/v1alpha1
kind: BackingStore
metadata:
  labels:
    app: noobaa
  name: noobaa-pv-backing-store
  namespace: openshift-storage
spec:
  pvPool:
    numVolumes: 1
    resources:
      requests:
        storage: "{{ ocp4_workload_logging_noobaa_pv_size }}"
    storageClass: "{{ ocp4_workload_logging_noobaa_storage_class }}"
  type: pv-pool